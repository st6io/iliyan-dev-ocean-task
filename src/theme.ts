import { defaultTheme } from '@xstyled/styled-components';

const theme = {
  ...defaultTheme,

  colors: {
    ...defaultTheme.colors,
    'light-grey': '#f8f8fa',
    primary: '#b41d76',
    text: '#353449',
  },
};

export default theme;
